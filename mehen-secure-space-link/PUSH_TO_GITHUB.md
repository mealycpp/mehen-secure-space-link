# Push this scaffold to GitHub

Repository:

```text
https://github.com/mealycpp/mehen-secure-space-link
```

From PowerShell or Git Bash:

```bash
git clone https://github.com/mealycpp/mehen-secure-space-link.git
cd mehen-secure-space-link
```

Copy the scaffold contents into the cloned folder, then:

```bash
git status
git add .
git commit -m "Initial MEHEN secure space-link release"
git branch -M main
git push -u origin main
```

Suggested repository topics:

```text
fprime, ascon, trng, aup-zu3, space-computing, flight-software, hardware-security, fpga, secure-telemetry
```
